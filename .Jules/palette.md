## 2025-05-18 - Icon-Only Button Accessibility
**Learning:** The application uses several icon-only buttons (e.g., mobile menu, language switcher) which consistently lack accessible labels.
**Action:** When working on UI components, specifically check all icon-only buttons for `aria-label` attributes and add them if missing.
