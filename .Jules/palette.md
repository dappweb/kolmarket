## 2026-01-31 - Icon-Only Buttons Missing Labels
**Learning:** Common pattern of icon-only buttons (mobile menu, language switcher) lacking `aria-label` and `aria-expanded` attributes. This makes navigation confusing for screen reader users.
**Action:** Audit all icon-only buttons and ensure they have descriptive `aria-label` attributes and state indicators where appropriate.
