## 2024-05-22 - Icon-Only Buttons Missing ARIA Labels
**Learning:** The navigation components (Header) rely heavily on icon-only buttons for mobile menu and language switching without providing accessible labels.
**Action:** Always verify icon-only interactive elements have `aria-label` or `aria-expanded` attributes during implementation.
